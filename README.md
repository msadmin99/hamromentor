# Hamromentor — Frontend

Next.js student-facing website. See the top-level `../README.md` for setup,
architecture, and demo credentials.

```bash
npm install
npm run dev   # http://localhost:3000
```
